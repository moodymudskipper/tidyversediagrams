
  # pkgs <- c(
  #   "ggplot2", "dplyr", "tidyr", "readr",
  #   "purrr", "tibble", "stringr", "forcats")
  # for (pkg in pkgs) {
  #   flow::flow_doc(pkg, out = paste0(pkg, ".md"), engine = "plantuml")
  # }
  # usethis::use_pkgdown()
  # pkgdown::build_site()
